# Core
numpy
pandas
scikit-learn
scipy

# Models
xgboost==2.0.3        # pinned for shap 0.49.1 compatibility; see README version note
tensorflow            # CPU on native Windows; use WSL2 for GPU

# Explainability
shap==0.49.1          # verified working with xgboost 2.0.3

# Figures
matplotlib

# Notes:
# - If you prefer a newer stack, use shap>=0.52 with xgboost>=3.x instead of the
#   pinned versions above. Do NOT mix shap 0.49.x with xgboost 3.x (TreeExplainer
#   fails to parse multiclass models).
# - Pillow is pulled in by matplotlib for image handling.